import React, { FC } from 'react'
import { Product } from '../models/models'

const Card: FC<Product> = ({id, name, price, category, source}) => {
  return (
    <div className='Card'>
        
    </div>
  )
}

export default Card